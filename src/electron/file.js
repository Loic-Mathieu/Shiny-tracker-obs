const fs = require('fs');

module.exports = {
    sendMessageTest: (message) => {
        console.log(message);
    },

    writeToFile: (path, fileName, content) => {
        // fs.watchFile();
    }
}
